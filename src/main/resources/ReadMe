此系统主要负责为服务器系统服务，主要业务有：
1、负责图片上传，将上传的byte数据转化为图片数据存储到磁盘中
2、图片访问地址的生成，需要将其记录到数据库中，以便访问
3、前端使用nginx处理静态页面，动态请求发送到servlet程序

4、图片存储目录生成规则：
	(1)、每个用户生成一个目录：用户量增加时，目录太多
	(1.1)、每天生成一个目录：
		每个目录中图片的存放数量设置最小值，只有在当天（23:59:59）目录中的图片超过这个值时，
			才生成新的目录供第二天使用。
	(2)、通过随机算法生成字符串（UUid+md5(userid)），并通过拆分字符串生成图片路径；
	(3)、用户头像使用单独目录存放
	(4)、使用单独的后台进程对图片数据进行备份（可以通过提交任务的方式）
	
	